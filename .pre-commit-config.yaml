repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        args: [--unsafe]
      - id: check-toml
      - id: check-ast
      - id: check-merge-conflict
      - id: check-added-large-files
      - id: detect-private-key

  - repo: https://github.com/PyCQA/bandit
    rev: 1.9.4
    hooks:
      - id: bandit
        name: Bandit security scan
        args: [--recursive, app, main.py, studio]
        pass_filenames: false

  - repo: local
    hooks:
      - id: no-regex
        name: Reject regular-expression usage
        entry: uv run python scripts/check_no_regex.py
        language: system
        types: [python]
      - id: compile-python
        name: Compile Python sources
        entry: uv run python -m compileall -q app main.py studio
        language: system
        pass_filenames: false
        types: [python]
